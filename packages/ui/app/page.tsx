export default function DemoPage() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
        gap: "1rem",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1>UI Package Demo</h1>
      <p>This workspace now exposes primitives only.</p>
    </main>
  );
}
